import React from 'react'
import ReactDOM from 'react-dom/client'

import './index.css'
import VideoBackgroundWith3DModel from './App.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VideoBackgroundWith3DModel/>
  </React.StrictMode>,
)
